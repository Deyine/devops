---
- name: Deploy Movie
  hosts: gestion-ticket
  tasks:
  - name: Cloner l'application
    git:
      repo: https://github.com/Deyine/laravel-movies-example.git
      dest: /var/www/laravel-movies-example
      clone: yes
      update: yes
  - name: restart apache2
    service: name=apache2 enabled=yes state=restarted
